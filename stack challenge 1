#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int stack[100];
int top = -1;
int maxSize;


void push(int value) {
    if (top == maxSize - 1) {
        printf("Overflow\n");
    } else {
        top++;
        stack[top] = value;
    }
}


void pop() {
    if (top == -1) {
        printf("Underflow\n");
    } else {
        printf("Popped: %d\n", stack[top]);
        top--;
    }
}


void display() {
    if (top == -1) {
        printf("Stack is Empty\n");
    } else {
        printf("Remaining Stack:\n");
        for (int i = top; i >= 0; i--) {
            printf("%d\n", stack[i]);
        }
    }
}

int main() {
    int N;
    scanf("%d", &N);
    maxSize = N;

    int a, b, c, d, e;

    
    scanf("%d %d %d", &a, &b, &c);
    push(a);
    push(b);
    push(c);

    
    pop();

    
    scanf("%d %d", &d, &e);
    push(d);
    push(e);

    
    pop();
    pop();
    pop();

   
    display();

    return 0;
}
