#include <stdio.h>

int main() {
    int n, i, x;
    int found = 0;

   
    scanf("%d", &n);

    int arr[1000];  

    
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

   
    scanf("%d", &x);

   
    for(i = 0; i < n; i++) {
        if(arr[i] == x) {
            found = 1;
            break;
        }
    }

    
    if(found == 1)
        printf("Found");
    else
        printf("Not Found");

    return 0;
}
