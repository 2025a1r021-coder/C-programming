#include <stdio.h>

int main() {
    int n, i, k, x, p, j;

    
    scanf("%d", &n);

    int arr[100];  

    
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
 
    scanf("%d", &k);

   
    for(i = 0; i < k; i++) {
        scanf("%d %d", &x, &p);

       
        for(j = n; j > p; j--) {
            arr[j] = arr[j - 1];
        }

       
        arr[p] = x;
        n++;  
    }

   
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
