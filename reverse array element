#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int arr[50];  // Since N <= 50

    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    // Print elements in reverse order
    for (int i = N - 1; i >= 0; i--) {
        printf("%d", arr[i]);
        if (i > 0) printf(" ");  // Add space between numbers, but not after the last
    }

    return 0;
}
