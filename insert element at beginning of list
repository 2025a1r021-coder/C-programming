#include <stdio.h>

int main() {
    int n, i, x;

    
    scanf("%d", &n);

    int arr[100];   

   
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    
    scanf("%d", &x);

    
    for(i = n; i > 0; i--) {
        arr[i] = arr[i - 1];
    }

    
    arr[0] = x;

    n++;   

    
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
